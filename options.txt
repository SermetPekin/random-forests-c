# Random Forest Options Example
# Lines starting with # are comments
n_estimators=100
max_depth=10
min_samples_leaf=2
max_features=auto
# Specify columns to include (by index, comma-separated, 0-based)
# all except last column 0:-1 , 
# 0:5 (columns between 1 and 4), 0:5,7:15 (columns between 1 and 5 and between 8 and 16) 
include_columns=0:5 
# 
# Specify target column (by index, 0-based)
target_column=-1 # last column 
